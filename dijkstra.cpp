#include <bits/stdc++.h>

using namespace std;

vector<pair <int,int> > grafoM[100000], grafoT[100000];
map<string, int> Lugar;
int main(){
    int aris, vertices, costo, tiempo;
    string a, b;
    cin >> aris >> vertices;
    for(int i=0; i<vertices; i++){
        cin >> a>> b>> costo >> tiempo;
        

    }
    
}